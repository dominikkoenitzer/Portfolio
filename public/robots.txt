# robots.txt for https://dk.punds.ch
# One group covers every crawler, search engines and AI crawlers alike.
#
# Do NOT add a per-bot group. A crawler obeys only the single most specific
# group that matches its token, so a "User-agent: GPTBot" block would replace
# this one for GPTBot entirely and silently drop the /api/ disallow rather than
# adding to it. Anything that should apply to a named bot has to be repeated
# inside its group. Every crawler is welcome here, so no named group is needed.

User-agent: *
Allow: /
Disallow: /api/

# Sitemap
Sitemap: https://dk.punds.ch/sitemap.xml

# AI / LLM resources (llms.txt convention)
# LLM: https://dk.punds.ch/llms.txt
# Full text: https://dk.punds.ch/llms-full.txt
