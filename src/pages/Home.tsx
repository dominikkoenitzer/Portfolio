import { PageLayout } from "@/components/layout/PageLayout";
import { HeroSection } from "@/components";

const Home = () => {
  return (
    <PageLayout>
      <HeroSection />
    </PageLayout>
  );
};

export default Home;

