## What does this PR do?

<!-- A brief summary of the change. -->

## Why?

<!-- Context, motivation, or linked issue (e.g. "Closes #12"). -->

## Checklist

- [ ] `bun run typecheck` passes
- [ ] `bun run build` passes
- [ ] Tested locally (`bun run dev`, port 1000)
- [ ] If page content changed, the matching SEO data (`src/config/seo-data/`) and translations (`src/lib/translations/`) were updated
- [ ] No secrets, tokens, or personal paths committed
